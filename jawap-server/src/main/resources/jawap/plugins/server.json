{
	"data": [
		{
			"title": "Server Thread Pool",
			"target": [
				"groupByNode(jawap.${application}.${instance}.${host}.server.threadpool.*.*, 7, 'maxSeries')"
			]
		}, {
			"title": "Number of Sessions",
			"target": [
				"jawap.${application}.${instance}.${host}.web.sessions.count"
			],
			"params": "areaMode=stacked"
		}
	]
}