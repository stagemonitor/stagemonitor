{
	"data": [
		{
			"title": "Heap",
			"target": [
				"alias(averageSeries(jawap.${application}.${environment}.${host}.jvm.memory.heap.max), 'max')",
				"alias(averageSeries(jawap.${application}.${environment}.${host}.jvm.memory.heap.committed), 'committed')",
				"alias(averageSeries(jawap.${application}.${environment}.${host}.jvm.memory.heap.used), 'used')"
			]
		},
		{
			"title": "Non-Heap",
			"target": [
				"alias(averageSeries(jawap.${application}.${environment}.${host}.jvm.memory.non-heap.max), 'max')",
				"alias(averageSeries(jawap.${application}.${environment}.${host}.jvm.memory.non-heap.committed), 'committed')",
				"alias(averageSeries(jawap.${application}.${environment}.${host}.jvm.memory.non-heap.used), 'used')"
			]
		},
		{
			"title": "GC",
			"target": [
				"groupByNode(nonNegativeDerivative(jawap.${application}.${environment}.${host}.jvm.gc.*.time), 6, 'sum')",
			]
		},
		{
			"title": "Memory Pools",
			"target": [
				"groupByNode(jawap.${application}.${environment}.${host}.jvm.memory.pools.*.usage, 7, 'averageSeries')",
			]
		}
	]
}